Rolling Wrench AI V11.1 — Service Call Toggle

Adds Service Call ON/OFF toggle to quote and invoice screens.
OFF = service call $0 for in-house work.
ON = uses default service call amount, usually $250.
